//PYTHON SOLUTION


class Solution:
    def shuffle(self, nums: List[int], n: int) -> List[int]:
     
        ans=[]
        l=len(nums)//2;
        for i in range(len(nums)//2):
            ans.append(nums[i]);
            ans.append(nums[i+l]);
        return ans;
