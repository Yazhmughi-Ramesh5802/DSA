class Solution {
    public int subtractProductAndSum(int n) {
        int prod=1;
        int sum=0;
        while(n!=0){
            int a = n%10;
            n/=10;
            prod*=a;
            sum+=a;

        }
        int ans = prod - sum;
        return ans;
    }
}
