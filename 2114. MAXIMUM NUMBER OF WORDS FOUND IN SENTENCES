class Solution {
    public int mostWordsFound(String[] sentences) {
        String[] sen;
        int size=0;
        for(int i=0;i<sentences.length;i++){
            sen = sentences[i].split(" ");
            if(sen.length>size) size = sen.length;
        }
        return size;
    }
}
