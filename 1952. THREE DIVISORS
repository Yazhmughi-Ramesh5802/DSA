class Solution {
    public boolean isThree(int n) {
        ArrayList <Integer> ans = new ArrayList<>();
        for(int i=1;i<=n;i++){
            if(n%i==0) ans.add(i);
        }
        if(ans.size()==3) return true;
        else return false;
    }
}
