class Solution:
    def isPalindrome(self, x: int) -> bool:
        if(x<0):
            return False
        s1=str(x)
        if(s1==s1[::-1]):
            return True
        else:
            return False
